import { useState } from 'react';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Box from '../components/Box/Box';
import SelectInput from '../components/SelectInput/SelectInput';
import Button from '../components/Button/Button';
import TextInput from '../components/TextInput/TextInput';
import Toggle from '../components/Toggle/Toggle';

<Meta title="Patterns/Form Controls" />

## Sizes

Sizes for form controls likes inputs and buttons, should match so that they can be placed inline.

<Canvas>
  <Story name="Sizes">
    {() => {
      const [value, setValue] = useState('');
      const [value1, setValue1] = useState('');
      const [value2, setValue2] = useState('');
      const [value3, setValue3] = useState({
        value: 'chocolate',
        label: 'Chocolate',
      });
      const [value4, setValue4] = useState();
      const options = [
        {
          value: 'chocolate',
          label: 'Chocolate',
        },
        {
          value: 'strawberry',
          label: 'Strawberry',
        },
        {
          value: 'vanilla',
          label: 'Vanilla',
        },
      ];
      return (
        <Box direction="row" childGap="xs">
          <Box childGap="sm" width="160px">
            <Box display="block">
              <TextInput
                id="smText"
                value={value}
                placeholder="Small"
                size="sm"
                onChange={event => setValue(event.target.value)}
              />
            </Box>
            <Box display="block">
              <TextInput
                id="mdText"
                value={value1}
                placeholder="Medium"
                onChange={event => setValue1(event.target.value)}
              />
            </Box>
            <Box>
              <TextInput
                id="lgText"
                value={value2}
                placeholder="Large"
                size="lg"
                onChange={event => setValue2(event.target.value)}
              />
            </Box>
          </Box>
          <Box childGap="sm">
            <Box display="block">
              <Button iconPrefix="property-agreement" size="sm">
                Small
              </Button>
            </Box>
            <Box display="block">
              <Button iconPrefix="property-agreement" size="md">
                Medium
              </Button>
            </Box>
            <Box display="block">
              <Button iconPrefix="property-agreement" size="lg">
                Large
              </Button>
            </Box>
          </Box>
          <Box childGap="sm" width="200px">
            <Box display="block">
              <SelectInput
                id="smallSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                size="sm"
              />
            </Box>
            <Box display="block">
              <SelectInput
                id="mediumSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                size="md"
              />
            </Box>
            <Box display="block">
              <SelectInput
                id="largeSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                size="lg"
              />
            </Box>
          </Box>
          <Box childGap="sm" width="200px">
            <Box display="block">
              <SelectInput
                id="smallMultiSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                isMulti
                size="sm"
              />
            </Box>
            <Box display="block">
              <SelectInput
                id="mediumMultiSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                isMulti
                size="md"
              />
            </Box>
            <Box display="block">
              <SelectInput
                id="largeMultiSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                isMulti
                size="lg"
              />
            </Box>
          </Box>
          <Box childGap="sm" width="160px">
            <Box height="32px" justifyContent="center">
              <Toggle
                id="smToggle"
                label="small toggle"
                hideLabel
                onChange={event => setValue4(event.target.checked)}
                isChecked={value4}
                size="sm"
              />
            </Box>
            <Box height="42px" justifyContent="center">
              <Toggle
                id="mdToggle"
                label="medium toggle"
                hideLabel
                onChange={event => setValue4(event.target.checked)}
                isChecked={value4}
                size="md"
              />
            </Box>
            <Box height="55px" justifyContent="center">
              <Toggle
                id="lgToggle"
                label="large toggle"
                hideLabel
                onChange={event => setValue4(event.target.checked)}
                isChecked={value4}
                size="lg"
              />
            </Box>
          </Box>
        </Box>
      );
    }}
  </Story>
</Canvas>

## Sizes with Labels

Inline form sizes with labels.

<Canvas>
  <Story name="Sizes with Labels">
    {() => {
      const [value, setValue] = useState('');
      const [value1, setValue1] = useState('');
      const [value2, setValue2] = useState('');
      const [value3, setValue3] = useState('');
      const [valueLabel4, setValueLabel4] = useState();
      const options = [
        {
          value: 'chocolate',
          label: 'Chocolate',
        },
        {
          value: 'strawberry',
          label: 'Strawberry',
        },
        {
          value: 'vanilla',
          label: 'Vanilla',
        },
      ];
      return (
        <div>
          <Box direction="row" childGap="sm" padding="sm" display="flex">
            <Box width="30">
              <TextInput
                id="smTextLabel"
                value={value}
                placeholder="Small"
                size="sm"
                label="Label"
                onChange={event => setValue(event.target.value)}
              />
            </Box>
            <Box alignItems="flex-end" direction="row">
              <Button iconPrefix="property-agreement" size="sm">
                Small Button
              </Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="smSelectLabel"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                size="sm"
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="smToggleLabel"
                label="small toggle"
                onChange={event => setValueLabel4(event.target.checked)}
                isChecked={valueLabel4}
                size="sm"
              />
            </Box>
          </Box>
          <Box direction="row" childGap="sm" padding="sm" display="flex">
            <Box width="30">
              <TextInput
                id="mdTextLabel"
                value={value1}
                placeholder="Medium"
                label="Label"
                onChange={event => setValue1(event.target.value)}
              />
            </Box>
            <Box alignItems="flex-end" direction="row">
              <Button iconPrefix="property-agreement">Medium Button</Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="mdSelectLabel"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                size="md"
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="mdToggleLabel"
                label="medium toggle"
                onChange={event => setValueLabel4(event.target.checked)}
                isChecked={valueLabel4}
                size="md"
              />
            </Box>
          </Box>
          <Box direction="row" childGap="sm" display="flex" padding="sm">
            <Box width="30">
              <TextInput
                id="lgTextLabel"
                value={value2}
                placeholder="Large"
                size="lg"
                label="Label"
                onChange={event => setValue2(event.target.value)}
              />
            </Box>
            <Box alignItems="flex-end" direction="row">
              <Button iconPrefix="property-agreement" size="lg">
                Large Button
              </Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="lgSelectLabel"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                size="lg"
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="lgToggleLabel"
                label="large toggle"
                onChange={event => setValueLabel4(event.target.checked)}
                isChecked={valueLabel4}
                size="lg"
              />
            </Box>
          </Box>
        </div>
      );
    }}
  </Story>
</Canvas>

## Sizes with Error States

Inline form sizes with error states.

<Canvas>
  <Story name="Sizes with Error State">
    {() => {
      const [value, setValue] = useState('');
      const [value1, setValue1] = useState('');
      const [value2, setValue2] = useState('');
      const [value3, setValue3] = useState();
      const [valueError4, setValueError4] = useState();
      const options = [
        {
          value: 'chocolate',
          label: 'Chocolate',
        },
        {
          value: 'strawberry',
          label: 'Strawberry',
        },
        {
          value: 'vanilla',
          label: 'Vanilla',
        },
      ];
      const errorMsg = 'Error message';
      return (
        <div>
          <Box direction="row" childGap="sm" padding="sm" display="flex">
            <Box width="30">
              <TextInput
                id="smTextError"
                value={value}
                placeholder="Small"
                size="sm"
                error={errorMsg}
                onChange={event => setValue(event.target.value)}
              />
            </Box>
            <Box alignItems="flex-start" direction="row">
              <Button iconPrefix="property-agreement" size="sm">
                Small Button
              </Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="smallErrorSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                size="sm"
                error={errorMsg}
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="smToggleError"
                label="small toggle"
                onChange={event => setValueError4(event.target.checked)}
                isChecked={valueError4}
                size="sm"
                hideLabel
                error={errorMsg}
              />
            </Box>
          </Box>
          <Box direction="row" childGap="sm" padding="sm" display="flex">
            <Box width="30">
              <TextInput
                id="mdTextError"
                value={value1}
                placeholder="Medium"
                error={errorMsg}
                onChange={event => setValue1(event.target.value)}
              />
            </Box>
            <Box alignItems="flex-start" direction="row">
              <Button iconPrefix="property-agreement">Medium Button</Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="mdErrorSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                error={errorMsg}
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="mdToggleError"
                label="small toggle"
                onChange={event => setValueError4(event.target.checked)}
                isChecked={valueError4}
                size="md"
                hideLabel
                error={errorMsg}
              />
            </Box>
          </Box>
          <Box direction="row" childGap="sm" display="flex" padding="sm">
            <Box width="30">
              <TextInput
                id="lgTextError"
                value={value2}
                placeholder="Large"
                size="lg"
                error={errorMsg}
                onChange={event => setValue2(event.target.value)}
              />
            </Box>
            <Box alignItems="flex-start" direction="row">
              <Button iconPrefix="property-agreement" size="lg">
                Large Button
              </Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="lgErrorSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                hideLabel
                size="lg"
                isClearable
                error={errorMsg}
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="lgToggleError"
                label="large toggle"
                onChange={event => setValueError4(event.target.checked)}
                isChecked={valueError4}
                size="lg"
                hideLabel
                error={errorMsg}
              />
            </Box>
          </Box>
        </div>
      );
    }}
  </Story>
</Canvas>

## Sizes with Labels and Error States

Inline form sizes with labels and error states.

<Canvas>
  <Story name="Sizes with Labels and Error State">
    {() => {
      const [value, setValue] = useState('');
      const [value1, setValue1] = useState('');
      const [value2, setValue2] = useState('');
      const [value3, setValue3] = useState('');
      const [valueErrorLabel4, setValueErrorLabel4] = useState();
      const options = [
        {
          value: 'chocolate',
          label: 'Chocolate',
        },
        {
          value: 'strawberry',
          label: 'Strawberry',
        },
        {
          value: 'vanilla',
          label: 'Vanilla',
        },
      ];
      const errorMsg = 'Error message';
      return (
        <div>
          <Box direction="row" childGap="sm" padding="sm" display="flex">
            <Box width="30">
              <TextInput
                id="smErrorLabelText"
                value={value}
                placeholder="Small"
                size="sm"
                label="Label"
                error={errorMsg}
                onChange={event => setValue(event.target.value)}
              />
            </Box>
            <Box alignItems="center" direction="row">
              <Button iconPrefix="property-agreement" size="sm">
                Small Button
              </Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="smErrorLabelSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                size="sm"
                error={errorMsg}
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="smToggleErrorLabel"
                label="small toggle"
                onChange={event => setValueErrorLabel4(event.target.checked)}
                isChecked={valueErrorLabel4}
                size="sm"
                error={errorMsg}
              />
            </Box>
          </Box>
          <Box direction="row" childGap="sm" padding="sm" display="flex">
            <Box width="30">
              <TextInput
                id="mdErrorLabelText"
                value={value1}
                placeholder="Medium"
                label="Label"
                error={errorMsg}
                onChange={event => setValue1(event.target.value)}
              />
            </Box>
            <Box alignItems="center" direction="row">
              <Button iconPrefix="property-agreement">Medium Button</Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="mdErrorLabelSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                size="md"
                error={errorMsg}
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="mdToggleErrorLabel"
                label="medium toggle"
                onChange={event => setValueErrorLabel4(event.target.checked)}
                isChecked={valueErrorLabel4}
                size="md"
                error={errorMsg}
              />
            </Box>
          </Box>
          <Box direction="row" childGap="sm" display="flex" padding="sm">
            <Box width="30">
              <TextInput
                id="lgErrorLabelText"
                value={value2}
                placeholder="Large"
                size="lg"
                label="Label"
                error={errorMsg}
                onChange={event => setValue2(event.target.value)}
              />
            </Box>
            <Box alignItems="center" direction="row">
              <Button iconPrefix="property-agreement" size="lg">
                Large Button
              </Button>
            </Box>
            <Box width="160px">
              <SelectInput
                id="lgSelect"
                label="Label"
                onChange={event => setValue3(event.target.value)}
                options={options}
                value={value3}
                size="lg"
                error={errorMsg}
              />
            </Box>
            <Box justifyContent="center">
              <Toggle
                id="lgToggleErrorLabel"
                label="large toggle"
                onChange={event => setValueErrorLabel4(event.target.checked)}
                isChecked={valueErrorLabel4}
                size="lg"
                error={errorMsg}
              />
            </Box>
          </Box>
        </div>
      );
    }}
  </Story>
</Canvas>
