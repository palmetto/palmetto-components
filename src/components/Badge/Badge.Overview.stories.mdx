import { useState } from 'react';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Badge } from './Badge';
import { Box } from '../Box/Box';
import { Button } from '../Button/Button';

<Meta title="Components/Badge/Overview" component={Badge} />

# Badge

Badges are used to display the status of an object or of an action that’s been taken.

<Canvas isExpanded>
  <Badge variant="default" message="Hello world!" />
</Canvas>

## Props

<ArgsTable of={Badge} />

## Variants

Badges come in different variants based on the kind of message being delivered to the user.

<Canvas>
  <Story name="Variants">
    {() => {
      const variants = [
        'default',
        'primary',
        'secondary',
        'tertiary',
        'info',
        'success',
        'warning',
        'danger',
      ];
      return (
        <Box direction="row" childGap="sm">
          {variants.map(variant => (
            <Badge message={variant} variant={variant} key={variant} />
          ))}
        </Box>
      );
    }}
  </Story>
</Canvas>

## Sizes

Set the size of the badge to `sm`, `md`, `lg` or `xl`. `md` is the default size. A `ResponsiveProp` object may also be used
so the badge can be a different size per breakpoint.

<Canvas>
  <Story name="Sizes">
    {() => {
      return (
        <>
          <Box direction="column" alignItems="flex-start" childGap="md">
            <Badge size="sm" message="Small" />
            <Badge size="md" message="Medium" />
            <Badge size="lg" message="Large" />
            <Badge size="xl" message="X-Large" />
            <Badge
              size={{
                base: 'sm',
                tablet: 'md',
                desktop: 'lg',
                hd: 'xl',
              }}
              message="Responsive"
            />
          </Box>
        </>
      );
    }}
  </Story>
</Canvas>

## Component Tokens

<table>
  <thead>
    <tr>
      <th>token name</th>
      <th>default value</th>
    </tr>
  </thead>
  <tbody>
    {(() => {
      const tokens = {
        '--badge-border-radius': '--size-border-radius-sm',
        '--badge-font-weight': '--size-font-weight-bold',
        '--badge-size-sm-font-size': '--size-font-2xs',
        '--badge-size-sm-padding-vertical': '--size-spacing-2xs',
        '--badge-size-sm-padding-horizontal': '--size-spacing-2xs',
        '--badge-size-md-font-size': '--size-font-xs',
        '--badge-size-md-padding-vertical': '--size-spacing-2xs',
        '--badge-size-md-padding-horizontal': '--size-spacing-xs',
        '--badge-size-lg-font-size': '--size-font-sm',
        '--badge-size-lg-padding-vertical': '--size-spacing-2xs',
        '--badge-size-lg-padding-horizontal': '--size-spacing-xs',
        '--badge-size-xl-font-size': '--size-font-md',
        '--badge-size-xl-padding-vertical': '--size-spacing-xs',
        '--badge-size-xl-padding-horizontal': '--size-spacing-sm',
        '--badge-primary-background-color': '--color-brand-primary-100',
        '--badge-primary-font-color': '--color-brand-grey-700',
        '--badge-secondary-background-color': '--color-brand-secondary-100',
        '--badge-secondary-font-color': '--color-brand-grey-700',
        '--badge-tertiary-background-color': '--color-brand-tertiary-100',
        '--badge-tertiary-font-color': '--color-brand-grey-700',
        '--badge-success-background-color': '--color-brand-success-100',
        '--badge-success-font-color': '--color-brand-grey-700',
        '--badge-danger-background-color': '--color-brand-danger-100',
        '--badge-danger-font-color': '--color-brand-grey-700',
        '--badge-info-background-color': '--color-brand-info-100',
        '--badge-info-font-color': '--color-brand-grey-700',
        '--badge-warning-background-color': '--color-brand-warning-100',
        '--badge-warning-font-color': '--color-brand-grey-700',
        '--badge-default-background-color': '--color-brand-grey-100',
        '--badge-default-font-color': '--color-brand-grey-700',
      };
      return Object.entries(tokens).map(([name, entry], i) => (
        <tr key={i}>
          <td>
            <code>{name}</code>
          </td>
          <td>
            <code>{entry}</code>
          </td>
        </tr>
      ));
    })()}
  </tbody>
</table>
