import Box from './Box';
import useBreakpoint from '../../hooks/useBreakpoint';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import {
  PALMETTO_FONT_SIZE_OPTIONS,
  PALMETTO_FONT_COLOR_OPTIONS,
  PALMETTO_BACKGROUND_COLOR_OPTIONS,
  PALMETTO_BORDER_COLOR_OPTIONS,
  PALMETTO_SPACING_SIZE_OPTIONS,
} from '../../lib/tokens';

<Meta title="Components/Box" component={Box} />

# Box

A `<Box>` is a layout component to build UIs with consistent padding and spacing between elements.

- It lays out it contents in a specified `direction`
- Renders a `<div />` element by default, but can be changed using `as`
- Allows you to specify the spacing between child elements using `childGap`

<Canvas>
  <Box childGap="md">
    <Box
      padding="xl"
      border="primary-light"
      background="primary-lightest"
      radius="md"
      fontSize="xl"
    >
      I am a box with a green background and border
    </Box>
    <Box
      childGap="sm"
      direction="row"
    >
      <Box flex="auto" radius="md" background="primary" height="lg" />
      <Box flex="auto" radius="md" background="primary" height="lg" />
      <Box flex="auto" radius="md" background="primary" height="lg" />
      <Box flex="auto" radius="md" background="primary" height="lg" />
      <Box flex="auto" radius="md" background="primary" height="lg" />
    </Box>
  </Box>
</Canvas>

## Props
<ArgsTable of={Box} />

## Background Color

Use the `background` prop to set the color of the background

<Canvas>
  <Story name="background color">
    <Box background="secondary-lighter" margin="xs" padding="xs">
      secondary-lighter
    </Box>
    <Box background="danger-lightest" margin="xs" padding="xs">
      danger-lightest
    </Box>
    <Box background="primary" margin="xs" padding="xs">
      primary
    </Box>
  </Story>
</Canvas>

All background colors

<Canvas>
  <Story name="all background colors">
    <Box flex="auto" direction="row" fontSize="sm">
      {[...PALMETTO_BACKGROUND_COLOR_OPTIONS].map((color, i) => {
        if (color.includes('inherit')) return;
        else if (color === 'dark'
          || color === 'light'
          || color === 'black'
          || color === 'white'
        ) {
          return (
            <Box flex="auto" padding="xs" background={color} key={i}>
              {`${color}`}
            </Box>
          );
        }
        return (
          <Box flex="auto" key={i}>
            <Box flex="auto" padding="xs" background={`${color}-lightest`}>
              {`${color}-lightest`}
            </Box>
            <Box flex="auto" padding="xs" background={`${color}-lighter`}>
              {`${color}-lighter`}
            </Box>
            <Box flex="auto" padding="xs" background={`${color}-light`}>
              {`${color}-light`}
            </Box>
            <Box flex="auto" padding="xs" background={color}>
              {`${color}`}
            </Box>
            <Box flex="auto" padding="xs" background={`${color}-dark`}>
              {`${color}-dark`}
            </Box>
            <Box flex="auto" padding="xs" background={`${color}-darker`}>
              {`${color}-darker`}
            </Box>
            <Box flex="auto" padding="xs" background={`${color}-darkest`}>
              {`${color}-darkest`}
            </Box>
          </Box>
        )
      })}
    </Box>
  </Story>
</Canvas>

## Border

Use the `border` prop to set the color of the border.

<Canvas>
  <Story name="border color">
    <Box childGap="xs">
      <Box border="grey-lighter"padding="xs">
        grey-lighter
      </Box>
      <Box border="danger" padding="xs">
        danger
      </Box>
      <Box border="primary-darker" padding="xs">
        primary-darker
      </Box>
    </Box>
  </Story>
</Canvas>

All border colors.

<Canvas>
  <Story name="all border colors">
     <Box direction="row" childGap="xs" fontSize="sm">
      {[...PALMETTO_BORDER_COLOR_OPTIONS].map((color, i) => {
        if (color.includes('inherit')) return;
        else if (color === 'dark'
          || color === 'light'
          || color === 'black'
          || color === 'white'
        ) {
          return (
            <Box border={color} padding="xs" key={i}>
              {`${color}`}
            </Box>
          );
        }
        return (
          <Box childGap="xs" key={i}>
            <Box flex="auto" border={`${color}-lightest`} padding="xs">
              {`${color}-lightest`}
            </Box>
            <Box flex="auto" border={`${color}-lighter`} padding="xs">
              {`${color}-lighter`}
            </Box>
            <Box flex="auto" border={`${color}-light`} padding="xs">
              {`${color}-light`}
            </Box>
            <Box flex="auto" border={color} padding="xs">
              {`${color}`}
            </Box>
            <Box flex="auto" border={`${color}-dark`} padding="xs">
              {`${color}-dark`}
            </Box>
            <Box flex="auto" border={`${color}-darker`} padding="xs">
              {`${color}-darker`}
            </Box>
            <Box flex="auto" border={`${color}-darkest`} padding="xs">
              {`${color}-darkest`}
            </Box>
          </Box>
        )
      })}
    </Box>
  </Story>
</Canvas>

### Child Gap

Set the spacing between children of the Box with `childGap`.
The space between children will be set whether the direction of is `row` or `column`.

<Canvas>
  <Story name="child gap">
    <Box childGap="md" padding="sm">
      <Box flex="auto" background="primary-light" padding="sm"></Box>
      <Box flex="auto" background="primary-light" padding="sm"></Box>
      <Box flex="auto" background="primary-light" padding="sm"></Box>
      <Box flex="auto" background="primary-light" padding="sm"></Box>
      <Box flex="auto" background="primary-light" padding="sm"></Box>
    </Box>
    <Box childGap="2xl" direction="row" padding="sm">
      <Box width="20%" background="primary-light" padding="sm"></Box>
      <Box width="20%" background="primary-light" padding="sm"></Box>
      <Box width="20%" background="primary-light" padding="sm"></Box>
      <Box width="20%" background="primary-light" padding="sm"></Box>
      <Box width="20%" background="primary-light" padding="sm"></Box>
    </Box>
  </Story>
</Canvas>

All child gap options.

<Canvas>
  <Story name="all child gap">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box childGap={spacing} direction="row" padding="sm" key={i}>
        <Box width="20%" background="primary-light" padding="sm">{spacing}</Box>
        <Box width="20%" background="primary-light" padding="sm">{spacing}</Box>
        <Box width="20%" background="primary-light" padding="sm">{spacing}</Box>
        <Box width="20%" background="primary-light" padding="sm">{spacing}</Box>
        <Box width="20%" background="primary-light" padding="sm">{spacing}</Box>
      </Box>
    ))}
  </Story>
</Canvas>

## Flex Properties

### Flex

The most common use case is to set children of a `<Box />` to `flex="auto"`.
The item is sized according to its width and height properties, but grows to absorb any extra free space in the parent, and shrinks to its minimum size to fit the container.
This is equivalent to setting "flex: 1 1 auto".

<Canvas>
  <Story name="flex auto">
    <Box childGap="md">
      <Box
        childGap="md"
        direction="row"
      >
        <Box flex="auto" background="primary-light" height="lg" justify="center" align="center">auto</Box>
        <Box flex="auto" background="primary-light" height="lg" justify="center" align="center">auto</Box>
        <Box flex="auto" background="primary-light" height="lg" justify="center" align="center">auto</Box>
        <Box flex="auto" background="primary-light" height="lg" justify="center" align="center">auto</Box>
        <Box flex="auto" background="primary-light" height="lg" justify="center" align="center">auto</Box>
      </Box>
    </Box>
  </Story>
</Canvas>

The `flex` prop can be used as shorthand for the flexbox css properties `flex-grow`, `flex-shrink`, `flex-basis`.

<Canvas>
  <Story name="flex grow and shrink">
    <Box childGap="md">
      <Box
        childGap="md"
        direction="row"
      >
        <Box flex="3" background="primary-light" height="lg" justify="center" align="center">3</Box>
        <Box flex="1" background="primary-light" height="lg" justify="center" align="center">1</Box>
        <Box flex="1" background="primary-light" height="lg" justify="center" align="center">1</Box>
        <Box flex="2" background="primary-light" height="lg" justify="center" align="center">2</Box>
        <Box flex="1" background="primary-light" height="lg" justify="center" align="center">1</Box>
      </Box>
    </Box>
  </Story>
</Canvas>

### Justify

Set content alignment along the main axis

<Canvas>
  <Story name="justify">
    <Box flex="auto" childGap="md">
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">start</Box>
        <Box flex="auto" background="primary-lighter" justify="start" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">center</Box>
        <Box flex="auto" background="primary-lighter" justify="center" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">end</Box>
        <Box flex="auto" background="primary-lighter" justify="end" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">between</Box>
        <Box flex="auto" background="primary-lighter" justify="between" direction="row"  childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">around</Box>
          <Box flex="auto" background="primary-lighter" justify="around" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">evenly</Box>
        <Box flex="auto" background="primary-lighter" justify="evenly" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
    </Box>
  </Story>
</Canvas>

### Alignment

Set content alignment along the cross axis.

<Canvas>
  <Story name="alignment">
    <Box flex="auto" childGap="md">
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">start</Box>
        <Box flex="auto" height="100px" background="primary-lighter" align="start" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">center</Box>
        <Box flex="auto" height="100px" background="primary-lighter" align="center" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">end</Box>
        <Box flex="auto" height="100px" background="primary-lighter" align="end" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">baseline</Box>
        <Box flex="auto" height="100px" background="primary-lighter" align="baseline" direction="row" childGap="md">
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
          <Box width="md" height="md" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box width="70px">stretch</Box>
        <Box flex="auto" height="100px" background="primary-lighter" align="stretch" direction="row" childGap="md">
          <Box width="md" background="primary" />
          <Box width="md" background="primary" />
          <Box width="md" background="primary" />
        </Box>
      </Box>
    </Box>
  </Story>
</Canvas>

### Align Content

Set the Box's lines within where there is extra space in the cross-axis.
This property has no effect when there is only one line of flex items.

<Canvas>
  <Story name="alignContent">
    <Box childGap="md">
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box flex="auto" width="70px">start</Box>
        <Box flex="auto" alignContent="start" height="200px" wrap childGap="md" background="primary-lighter" direction="row" childGap="md">
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box flex="auto" width="70px">center</Box>
        <Box flex="auto" alignContent="center" height="200px" wrap childGap="md" background="primary-lighter" direction="row" childGap="md">
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box flex="auto" width="70px">end</Box>
        <Box flex="auto" alignContent="end" height="200px" wrap childGap="md" background="primary-lighter" direction="row" childGap="md">
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box flex="auto" width="70px">stretch</Box>
        <Box flex="auto" alignContent="stretch" height="200px" wrap childGap="md" background="primary-lighter" direction="row" childGap="md">
          <Box width="2xl" margin="0 0 md 0" background="primary" />
          <Box width="2xl" margin="0 0 md 0" background="primary" />
          <Box width="2xl" margin="0 0 md 0" background="primary" />
          <Box width="2xl" margin="0 0 md 0" background="primary" />
          <Box width="2xl" margin="0 0 md 0" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box flex="auto" width="70px">between</Box>
        <Box flex="auto" alignContent="between" height="200px" wrap childGap="md" background="primary-lighter" direction="row" childGap="md">
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
        </Box>
      </Box>
      <Box direction="row" align="center" direction="row" childGap="md">
        <Box flex="auto" width="70px">around</Box>
        <Box flex="auto"  alignContent="center" height="200px" wrap childGap="md" background="primary-lighter" direction="row" childGap="md">
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
          <Box width="2xl" height="md" margin="0 0 md 0" background="primary" />
        </Box>
      </Box>
    </Box>
  </Story>
</Canvas>

## Font Color

Use the `color` prop to set the color of the text

<Canvas>
  <Story name="color">
     {[...PALMETTO_FONT_COLOR_OPTIONS].map((color, i) => (
      <Box color={color} background={color === 'inverse' ? 'grey-darker' : null} key={i}>
        {`${color}`}
      </Box>
    ))}
  </Story>
</Canvas>

## Font Size

Set the `fontSize` prop with a [font size](/?path=/story/design-tokens-font-size--page) token to set the size of the text in the Box.

<Canvas>
  <Story name="fontSize">
    {[...PALMETTO_FONT_SIZE_OPTIONS].reverse().map((fontSize, i) => (
      <Box fontSize={fontSize} key={i}>
        {`${fontSize} font size`}
      </Box>
    ))}
  </Story>
</Canvas>

## Margin

The `margin` prop accepts a single [spacing](/?path=/story/design-tokens-spacing--page) value or a set of spacing values that correspond with css shorthand syntax. Margin values can be set on the horizontal or vertical axis, or the top, right, bottom and left sides of a Box.

<Canvas>
  <Story name="margin">
    <Box margin="lg md" background="primary-lighter">
      lg vertical and md horizontal
    </Box>
     <Box margin="0 2xl" background="primary-lighter">
      0 vertical and 2xl horizontal
    </Box>
    <Box margin="sm md 2xl" background="primary-lighter">
      sm top, md horizontal, 2xl bottom
    </Box>
    <Box margin="sm lg 2xl 0" background="primary-lighter">
      sm top, lg right, 2xl bottom, 0 left
    </Box>
  </Story>
</Canvas>

A single `margin` value will apply an equal amount of space around the outside of a Box.

<Canvas>
  <Story name="single margin">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box margin={spacing} background="primary-lighter" key={i}>
        {`${spacing} margin`}
      </Box>
    ))}
  </Story>
</Canvas>

### Horizontal Margin

<Canvas>
  <Story name="margin horizontal">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box margin={`0 ${spacing}`} background="primary-lighter" key={i}>
        {`${spacing} horizontal margin`}
      </Box>
    ))}
  </Story>
</Canvas>

### Vertical Margin

<Canvas>
  <Story name="margin vertical">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box margin={`${spacing} 0`} background="primary-lighter" key={i}>
        {`${spacing} vertical margin`}
      </Box>
    ))}
  </Story>
</Canvas>

## Padding

The `padding` prop accepts a single [spacing](/?path=/story/design-tokens-spacing--page) value or a set of spacing values that correspond with css shorthand syntax. Padding values can be set on the horizontal or vertical axis, or the top, right, bottom and left sides of a Box.

<Canvas>
  <Story name="padding">
    <Box padding="lg md" margin="sm 0" background="primary-lighter">
      lg vertical and md horizontal
    </Box>
     <Box padding="0 2xl" margin="sm 0" background="primary-lighter">
      0 vertical and 2xl horizontal
    </Box>
    <Box padding="sm md 2xl" margin="sm 0" background="primary-lighter">
      sm top, md horizontal, 2xl bottom
    </Box>
    <Box padding="sm lg 2xl 0" margin="sm 0" background="primary-lighter">
      sm top, lg right, 2xl bottom, 0 left
    </Box>
  </Story>
</Canvas>

A single `padding` value will apply an equal amount of space around the contents of a Box.

<Canvas>
  <Story name="single padding">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box padding={spacing} background="primary-lighter" margin="xs 0" key={i}>
        {`${spacing} padding`}
      </Box>
    ))}
  </Story>
</Canvas>



### Horizontal Padding

<Canvas>
  <Story name="padding horizontal">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box padding={`0 ${spacing}`} background="primary-lighter" margin="xs 0" key={i}>
        {`${spacing} horizontal padding`}
      </Box>
    ))}
  </Story>
</Canvas>

### Vertical Padding

<Canvas>
  <Story name="padding vertical">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box padding={`${spacing} 0`} background="primary-lighter" margin="xs 0" key={i}>
        {`${spacing} vertical padding`}
      </Box>
    ))}
  </Story>
</Canvas>


## Radius

Set the `radius` prop with a [radius token](/?path=/docs/design-tokens-border-radius--page) to set the corner radius of the box.

<Canvas>
  <Story name="radius">
    <>
      <Box radius="xs" background="secondary-light" padding="sm" margin="xs 0">
        sm radius
      </Box>
      <Box radius="sm" background="secondary-light" padding="sm" margin="xs 0">
        md radius
      </Box>
      <Box radius="md" background="secondary-light" padding="sm" margin="xs 0">
        lg radius
      </Box>
      <Box radius="lg" background="secondary-light" padding="sm" margin="xs 0">
        xl radius
      </Box>
      <Box radius="circle" background="secondary-light" padding="sm" margin="xs 0">
        circle radius
      </Box>
    </>
  </Story>
</Canvas>

## Width

Width can be set to any CSS unit value (px, rem, em, %).

<Canvas>
  <Story name="width">
    <Box background="primary-lightest" childGap="xs">
      <Box background="primary-light" width="227px">227px</Box>
      <Box background="primary-light" width="10rem">10rem</Box>
      <Box background="primary-light" width="50%">50%</Box>
    </Box>
  </Story>
</Canvas>

[Width tokens](/?path=/docs/design-tokens-width--page) may be used to set widths as well.

<Canvas>
  <Story name="width standards">
    <Box background="primary-lightest" childGap="xs" overflow="auto">
      <Box background="primary-light" width="sm">sm</Box>
      <Box background="primary-light" width="md">md</Box>
      <Box background="primary-light" width="lg">lg</Box>
      <Box background="primary-light" width="xl">xl</Box>
      <Box background="primary-light" width="2xl">2xl</Box>
      <Box background="primary-light" width="3xl">3xl</Box>
      <Box background="primary-light" width="4xl">4xl</Box>
      <Box background="primary-light" width="5xl">5xl</Box>
    </Box>
  </Story>
</Canvas>


Set the width to a common percentage of the Box's parent.

<Canvas>
  <Story name="width percentage">
    <Box background="primary-lightest" childGap="xs">
      <Box background="primary-light" width="10">10%</Box>
      <Box background="primary-light" width="15">15%</Box>
      <Box background="primary-light" width="20">20%</Box>
      <Box background="primary-light" width="25">25%</Box>
      <Box background="primary-light" width="30">30%</Box>
      <Box background="primary-light" width="33">33%</Box>
      <Box background="primary-light" width="34">34%</Box>
      <Box background="primary-light" width="40">40%</Box>
      <Box background="primary-light" width="50">50%</Box>
      <Box background="primary-light" width="60">60%</Box>
      <Box background="primary-light" width="70">70%</Box>
      <Box background="primary-light" width="75">75%</Box>
      <Box background="primary-light" width="80">80%</Box>
      <Box background="primary-light" width="90">90%</Box>
      <Box background="primary-light" width="100">100%</Box>
    </Box>
  </Story>
</Canvas>

## Max Width

Set the maximum width of the Box by setting `maxWidth` to a [width token](/?path=/docs/design-tokens-width--page) or any valid css unit.

<Canvas>
  <Story name="max width">
    <Box background="primary-lightest" childGap="xs">
      <Box background="primary-light" maxWidth="2xl">2xl</Box>
      <Box background="primary-light" maxWidth="145px">145px</Box>
      <Box background="primary-light" maxWidth="50%">50%</Box>
    </Box>
  </Story>
</Canvas>

## Height

Height can be set to any CSS unit value (px, rem, em, %).

<Canvas>
  <Story name="height">
    <Box background="primary-lightest" childGap="xs">
      <Box background="primary-light" height="227px" justify="center" align="center">227px</Box>
      <Box background="primary-light" height="10rem" justify="center" align="center">10rem</Box>
      <Box background="primary-light" height="4em" justify="center" align="center">4em</Box>
    </Box>
  </Story>
</Canvas>

[Width tokens](/?path=/docs/design-tokens-height--page) may be used to set height as well.

<Canvas>
  <Story name="height standards">
    <Box background="primary-lightest" childGap="xs" height="5xl" direction="row">
      <Box flex="auto" background="primary-light" justify="center" align="center" height="sm">sm</Box>
      <Box flex="auto" background="primary-light" justify="center" align="center" height="md">md</Box>
      <Box flex="auto" background="primary-light" justify="center" align="center" height="lg">lg</Box>
      <Box flex="auto" background="primary-light" justify="center" align="center" height="xl">xl</Box>
      <Box flex="auto" background="primary-light" justify="center" align="center" height="2xl">2xl</Box>
      <Box flex="auto" background="primary-light" justify="center" align="center" height="3xl">3xl</Box>
      <Box flex="auto" background="primary-light" justify="center" align="center" height="4xl">4xl</Box>
      <Box flex="auto" background="primary-light" justify="center" align="center" height="5xl">5xl</Box>
    </Box>
  </Story>
</Canvas>

Set the height to a common percentage of the Box's parent.

<Canvas>
  <Story name="height percentage">
    <Box height="400px" childGap="xs" direction="row" background="primary-lightest">
      <Box flex="auto" justify="center" align="center" background="primary-light" height="10">10%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="15">15%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="20">20%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="25">25%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="30">30%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="33">33%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="34">34%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="40">40%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="50">50%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="60">60%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="70">70%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="75">75%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="80">80%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="90">90%</Box>
      <Box flex="auto" justify="center" align="center" background="primary-light" height="100">100%</Box>
    </Box>
  </Story>
</Canvas>

## Max Height

Set the maximum width of the Box by setting `maxHeight` to a [height token](/?path=/docs/design-tokens-height--page) or any valid css unit.

<Canvas>
  <Story name="max height">
    <Box height="300px" background="primary-lightest" direction="row" childGap="xs">
      <Box maxHeight="lg" flex="auto" justify="center" align="center" background="primary-light">lg</Box>
      <Box maxHeight="50px" flex="auto" justify="center" align="center" background="primary-light">50px</Box>
      <Box maxHeight="50%" flex="auto" justify="center" align="center" background="primary-light">50%</Box>
    </Box>
  </Story>
</Canvas>

### Overflow

Set the overlfow behavior of the Box with `overflow`

<Canvas>
  <Story name="overflow">
    <Box overflow="auto" padding="md" childGap="md" flex="auto" background="primary-lighter" border="grey" height="100px">
      <p>Most homeowners are stuck with a single option for their power company, and that utility just keeps increasing rates year after year. With solar, you take back control over where your energy comes from, how much it costs, and what you can do with it. Use it, store it, or sell it, all on your own terms.</p>
      <p>Solar can be complicated, but Palmetto makes it easy. We have solar, storage, and maintenance, all under one roof. Plus, you can monitor your entire system from the palm of your hand, and we have an award-winning customer service team standing by to help you with anything you might need.</p>
      <p>Switching to solar saves the average Palmetto customer $18,471 over 25 years. Add in national tax credits and local rebates and incentives, and you can save even more. Like many things in life though, these discounts won’t be around forever, so take advantage of them while you can.</p>
    </Box>
  </Story>
</Canvas>

## Responsive Styling

Props that are responsive-enabled follow a mobile-first approach, which means that styles are applied first to smaller viewports (i.e. mobile devices).
As the viewport size increases, new styles may be applied (styles are overidden for larger viewports).
Responsive enabled props take a keyed object that specifies at which [breakpoints](/?path=/docs/design-tokens-breakpoints--page) to apply certain properties.

```
<Box
  width={{
    base: '100px' // box width is 100px
    tablet: '200px' // box width is 200px when viewport width is greater than size-breakpoint-tablet
    desktop: '300px' // box width is 600px when viewport width is greater than size-breakpoint-destkop
    hd: '600px' // box width is 600px when viewport width is greater than size-breakpoint-hd
  }}
>
```

<Canvas>
  <Story name="responsive padding and margin">
    <>
    <p>This box will have different margin and padding values for each of its base, tablet, and desktop breakpoints.</p>
    <Box
      padding={{ base: 'sm 2xs', tablet: '2xl lg', desktop: '5xl 3xl'}}
      margin={{ base: 'sm xs', tablet: 'md sm', desktop: 'lg md'}}
      childGap="md"
      flex="auto"
      background="primary-lighter"
      border="grey"
    >
      <p>Most homeowners are stuck with a single option for their power company, and that utility just keeps increasing rates year after year. With solar, you take back control over where your energy comes from, how much it costs, and what you can do with it. Use it, store it, or sell it, all on your own terms.</p>
      <p>Solar can be complicated, but Palmetto makes it easy. We have solar, storage, and maintenance, all under one roof. Plus, you can monitor your entire system from the palm of your hand, and we have an award-winning customer service team standing by to help you with anything you might need.</p>
      <p>Switching to solar saves the average Palmetto customer $18,471 over 25 years. Add in national tax credits and local rebates and incentives, and you can save even more. Like many things in life though, these discounts won’t be around forever, so take advantage of them while you can.</p>
    </Box>
    </>
  </Story>
</Canvas>

### Omitting Keys

When passing an object, omitting a key is the same as not applying any styles.
For example the following has padding at the `desktop` breakpoint and wider.

<Canvas>
  <Story name="responsive padding lg">
    <Box
      padding={{ desktop: 'lg'}}
      background="primary-lighter"
    >
      <p>This box will have no padding until the viewport reaches the desktop breakpoint. </p>
    </Box>
  </Story>
</Canvas>

### Responsive Examples

Try resizing the browser to see the responsive properties in action.

### Direction

<Canvas>
  <Story name="responsive direction">
    {() => {
      const activeBreakpoint = useBreakpoint();
      const direction = { base: 'column', tablet: 'column', desktop: 'row', hd: 'row' };
      return (
        <>
          <Box
            direction={direction}
            childGap="lg"
          >
            <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
              {direction[activeBreakpoint.name]}
            </Box>
            <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
              {direction[activeBreakpoint.name]}
            </Box>
            <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
              {direction[activeBreakpoint.name]}
            </Box>
          </Box>
        </>
      );
    }}
  </Story>
</Canvas>

### Font Size

This box will have different fontSize values for each of its base, tablet, desktop, and hd breakpoints.

<Canvas>
  <Story name="responsive font size">
    {() => {
      const activeBreakpoint = useBreakpoint();
      const fontSize = { base: 'md', tablet: 'lg', desktop: 'xl', hd: '4xl' };
      return (
        <Box
          padding="sm"
          background="primary-lighter"
          border="grey"
          fontSize={fontSize}
        >
          <p>Breakpoint: {activeBreakpoint.name}</p>
          <p>fontSize: {fontSize[activeBreakpoint.name]}</p>
        </Box>
      );
    }}
  </Story>
</Canvas>

### Width and Height

This box will have different width and height values for each of its base, tablet, and desktop breakpoints.

<Canvas>
  <Story name="responsive width and height">
    {() => {
      const activeBreakpoint = useBreakpoint();
      const width = { base: '50%', tablet: '60%', desktop: '70%', hd: '100%' };
      const height = { base: '100px', tablet: '200px', desktop: '300px', hd: '400px' };
      return (
        <Box
          padding="sm"
          background="primary-lighter"
          border="grey"
          height={height}
          width={width}
        >
          <p>Breakpoint: {activeBreakpoint.name}</p>
          <p>Width: {width[activeBreakpoint.name]}</p>
          <p>Height: {height[activeBreakpoint.name]}</p>
        </Box>
      );
    }}
  </Story>
</Canvas>

### Child gap

The childGap in these boxes will change based on the active breakpoint

<Canvas>
  <Story name="responsive gaps">
    {() => {
      const activeBreakpoint = useBreakpoint();
      const childGap = { base: 'sm', tablet: 'lg', desktop: '2xl', hd: '5xl' };
      return (
        <Box
          childGap={childGap}
          direction="row"
        >
          <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
            {childGap[activeBreakpoint.name]}
          </Box>
          <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
            {childGap[activeBreakpoint.name]}
          </Box>
          <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
            {childGap[activeBreakpoint.name]}
          </Box>
          <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
            {childGap[activeBreakpoint.name]}
          </Box>
          <Box flex="auto" radius="md" background="primary-lighter" height="lg" justifyContent="center" align="center">
            {childGap[activeBreakpoint.name]}
          </Box>
        </Box>
      );
    }}
  </Story>
</Canvas>

### Radius

This box will have different border radius based on the breakpoint

<Canvas>
  <Story name="responsive radius">
    {() => {
      const activeBreakpoint = useBreakpoint();
      const radius = { base: 'circle', tablet: 'sm', desktop: 'md', hd: 'lg' };
      return (
        <Box
          padding="sm"
          margin="md"
          background="primary-lighter"
          border="grey"
          radius={radius}
        >
          <p>{radius[activeBreakpoint.name]}</p>
        </Box>
      );
    }}
  </Story>
</Canvas>

