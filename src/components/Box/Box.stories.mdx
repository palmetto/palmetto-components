
import Box from './Box';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {
  PALMETTO_FONT_SIZE_OPTIONS,
  PALMETTO_FONT_COLOR_OPTIONS,
  PALMETTO_BRAND_COLOR_OPTIONS,
  PALMETTO_BACKGROUND_COLOR_OPTIONS,
  PALMETTO_SPACING_SIZE_OPTIONS,
} from '../../lib/tokens';

<Meta title="Components/Box" component={Box} />

# Box

A `<Box>` is a layout component to build UIs with consistent padding and spacing between elements.
It lays out it contents in a specified direction, and by default renders a `div` element.

<Preview>
  <>
    <Box
      margin="lg"
      padding="xl"
      border="grey-lighter"
      radius="md"
      fontSize="2xl"
    >
      I am a Box
    </Box>
    <Box
      margin="lg"
      padding="md"
      background="info-lighter"
      color="info"
      radius="md"
    >
      I am a Box too!
    </Box>
    <Box
      margin="lg"
      padding="md"
      background="grey-dark"
      color="inverse"
      fontSize="sm"
    >
      I am an inverse Box
    </Box>
  </>
</Preview>

## Props
<Props of={Box} />

## Background Color

Use the `background` prop to set the color of the background

<Preview>
  <Story name="background">
     {[...PALMETTO_BACKGROUND_COLOR_OPTIONS].map((color, i) => {
      if (color.includes('inherit')) return;
      else if (color === 'dark'
        || color === 'light'
        || color === 'black'
        || color === 'white'
      ) {
        return (
          <Box padding="xs" background={color} key={i}>
            {`${color} background`}
          </Box>
        );
      }
      return (
        <>
          <Box padding="xs" background={`${color}-lightest`} key={i}>
            {`${color}-lightest background`}
          </Box>
          <Box padding="xs" background={`${color}-lighter`} key={i}>
            {`${color}-lighter background`}
          </Box>
          <Box padding="xs" background={`${color}-light`} key={i}>
            {`${color}-light background`}
          </Box>
          <Box padding="xs" background={color} key={i}>
            {`${color} background`}
          </Box>
          <Box padding="xs" background={`${color}-dark`} key={i}>
            {`${color}-dark background`}
          </Box>
          <Box padding="xs" background={`${color}-darker`} key={i}>
            {`${color}-darker background`}
          </Box>
          <Box padding="xs" background={`${color}-darkest`} key={i}>
            {`${color}-darkest background`}
          </Box>
        </>
      )
    })}
  </Story>
</Preview>

## Border

Use the `border` prop to set the color of the border

<Preview>
  <Story name="border color">
     {[...PALMETTO_BRAND_COLOR_OPTIONS].map((color, i) => {
      if (color.includes('inherit')) return;
      else if (color === 'dark'
        || color === 'light'
        || color === 'black'
        || color === 'white'
      ) {
        return (
          <Box border={color} margin="xs" padding="xs" key={i}>
            {`${color} border`}
          </Box>
        );
      }
      return (
        <>
          <Box border={`${color}-lightest`} margin="xs" padding="xs" key={i}>
            {`${color}-lightest border`}
          </Box>
          <Box border={`${color}-lighter`} margin="xs" padding="xs" key={i}>
            {`${color}-lighter border`}
          </Box>
          <Box border={`${color}-light`} margin="xs" padding="xs" key={i}>
            {`${color}-light border`}
          </Box>
          <Box border={color} margin="xs" padding="xs" key={i}>
            {`${color} border`}
          </Box>
          <Box border={`${color}-dark`} margin="xs" padding="xs" key={i}>
            {`${color}-dark border`}
          </Box>
          <Box border={`${color}-darker`} margin="xs" padding="xs" key={i}>
            {`${color}-darker border`}
          </Box>
          <Box border={`${color}-darkest`} margin="xs" padding="xs" key={i}>
            {`${color}-darkest border`}
          </Box>
        </>
      )
    })}
  </Story>
</Preview>

## Font Color

Use the `color` prop to set the color of the text

<Preview>
  <Story name="color">
     {[...PALMETTO_FONT_COLOR_OPTIONS].map((color, i) => (
      <Box color={color} background={color === 'inverse' ? 'grey-darker' : null} key={i}>
        {`${color}`}
      </Box>
    ))}
  </Story>
</Preview>

## Font Size

Use the `fontSize` prop to set the size of the text

<Preview>
  <Story name="fontSize">
    {[...PALMETTO_FONT_SIZE_OPTIONS].reverse().map((fontSize, i) => (
      <Box fontSize={fontSize} key={i}>
        {`${fontSize} font size`}
      </Box>
    ))}
  </Story>
</Preview>

## Margin

Set a single spacing value for equal margin around the outside of the box.

<Preview>
  <Story name="margin">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box margin={spacing} background="primary-lighter" key={i}>
        {`${spacing} margin`}
      </Box>
    ))}
  </Story>
</Preview>

### Horizontal Margin

<Preview>
  <Story name="margin horizontal">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box margin={`0 ${spacing}`} background="primary-lighter" key={i}>
        {`${spacing} horizontal margin`}
      </Box>
    ))}
  </Story>
</Preview>

### Vertical Margin

<Preview>
  <Story name="margin vertical">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box margin={`${spacing} 0`} background="primary-lighter" key={i}>
        {`${spacing} vertical margin`}
      </Box>
    ))}
  </Story>
</Preview>

## Padding

Set a single spacing value for equal padding around the contents of the box.

<Preview>
  <Story name="padding">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box padding={spacing} background="primary-lighter" margin="xs 0" key={i}>
        {`${spacing} padding`}
      </Box>
    ))}
  </Story>
</Preview>

### Horizontal Padding

<Preview>
  <Story name="padding horizontal">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box padding={`0 ${spacing}`} background="primary-lighter" margin="xs 0" key={i}>
        {`${spacing} padding margin`}
      </Box>
    ))}
  </Story>
</Preview>

### Vertical Padding

<Preview>
  <Story name="padding vertical">
    {[...PALMETTO_SPACING_SIZE_OPTIONS].map((spacing, i) => (
      <Box padding={`${spacing} 0`} background="primary-lighter" margin="xs 0" key={i}>
        {`${spacing} padding margin`}
      </Box>
    ))}
  </Story>
</Preview>

## Radius

Use the `radius` prop to set the corner radius of the box.

<Preview>
  <Story name="radius">
    <>
      <Box radius="xs" background="secondary-light" padding="sm" margin="xs 0">
        sm radius
      </Box>
      <Box radius="sm" background="secondary-light" padding="sm" margin="xs 0">
        md radius
      </Box>
      <Box radius="md" background="secondary-light" padding="sm" margin="xs 0">
        lg radius
      </Box>
      <Box radius="lg" background="secondary-light" padding="sm" margin="xs 0">
        xl radius
      </Box>
    </>
  </Story>
</Preview>
