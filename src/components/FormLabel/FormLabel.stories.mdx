import FormLabel from './FormLabel';
import { action } from '@storybook/addon-actions';
import { withA11y } from '@storybook/addon-a11y';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta
  title="Components/Form Inputs/Subcomponents/FormLabel"
  component={FormLabel}
  decorators={[withA11y]}
/>

# FormLabel

FormLabel is a subcomponent of form inputs and is not intended to be used directly.

## Props

<Props of={FormLabel} />

## Default

<Preview>
  <Story name="Default">
    <FormLabel labelText="input label" inputId="name" />
  </Story>
</Preview>

## Required

<Preview>
  <Story name="Required">
    <FormLabel labelText="required input label" inputId="name" isFieldRequired />
  </Story>
</Preview>

## Error States

<Preview>
  <Story name="Error States">
    <>
    <FormLabel labelText="input label has error" inputId="name" hasError />
    <FormLabel labelText="required input label has error" inputId="name" hasError isFieldRequired />
    </>
  </Story>
</Preview>
