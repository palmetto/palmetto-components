import { Meta } from '@storybook/addon-docs/blocks';
import { PALMETTO_SPACING_OPTIONS, PALMETTO_SPACING_VALUES } from '../lib/tokens';

<Meta
  title="Design Tokens/Spacing"
/>

# Spacing

Spacing tokens are intended for use with `margin` and `padding` properties. They are defined in `rem`, which means they will scale when adjusting the root font size. Pixel values are calculated with a root font size of `16px`.

<table>
  <thead>
    <tr>
      <th>token name</th>
      <th>prop value</th>
      <th>value</th>
      <th>px</th>
      <th>notes</th>
    </tr>
  </thead>
  <tbody>
    {PALMETTO_SPACING_OPTIONS.map((space, i) => (
      <tr key={i}>
        <td><code>{`size-spacing-${space}`}</code></td>
        <td><code>{space}</code></td>
        <td>{PALMETTO_SPACING_VALUES[i].value}</td>
        <td>{space === 'base' ? '16' : PALMETTO_SPACING_VALUES[i].original.value * 16}</td>
        <td>{PALMETTO_SPACING_VALUES[i].original.comment}</td>
      </tr>
    ))}
  </tbody>
</table>

